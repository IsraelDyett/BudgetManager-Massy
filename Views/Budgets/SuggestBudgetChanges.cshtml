@model BudgetManager.Models.Budget

@{
    ViewData["Title"] = "SuggestBudgetChanges";
}

<h1>SuggestBudgetChanges</h1>

<h4>Budget</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="SuggestBudgetChanges">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BudgetID" />
            
            <div class="form-group">
                <label asp-for="SuggestedChanges" class="control-label"></label>
                <input asp-for="SuggestedChanges" class="form-control" />
                <span asp-validation-for="SuggestedChanges" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
